const numbers = [1,2,3]; //numbers will hold values 1,2,3
const newNumbers = [...numbers, 5]; //(...numbers will hold 1,2,3 and newly added 5 will be added to the newly assigned variable newNumbers)
console.log(newNumbers);//print the values stored in the newnumbers variable